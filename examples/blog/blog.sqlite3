CREATE TABLE posts (
  id integer primary key,
  user_id integer,
  title text,
  body text,
  at date
);

CREATE TABLE users (
  id integer primary key,
  username text,
  password text
);

CREATE TABLE comments (
  id integer primary key,
  post_id integer,
  username text,
  body text,
  created_at date
);
